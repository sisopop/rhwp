# 2026-06-01 — 오늘 할일

## PR #1193 (Fix mac window resizing, @chkwon — rhwp 첫 기여자) — 완료

- PR #1193 → **MERGED** (devel `8a2e60ca`). 연결 이슈 #1191 → CLOSED (`closes #1191`, cross-repo `--no-ff` 라 수동 클로즈).
- 본질: `#editor-area` grid 트랙 `1fr`(=`minmax(auto,1fr)` 콘텐츠 최소 래칫) → `minmax(0, 1fr)` 로 변경. macOS 창 넓혔다 줄일 때 가운데 정렬 깨지는 #1191 해소. CSS 정석 해결책, 1파일 +5/-2.
- 검증: 로컬 머지 CLEAN / `npm run build`(tsc+vite) / WASM Docker 빌드. **작업지시자 시각 판정 통과**(창 리사이즈 가운데 정렬 유지).
- **@chkwon 첫 기여 → 따뜻한 환영 코멘트** 게시 (feedback_pr_comment_tone 취지와 별개로 첫 기여 환영은 적절).
- 보고서: pr_1193_report.md / 검토서: pr_1193_review.md

## Task #1189 — task 1139 후속 보정

- [x] Stage6 시작: `3-11월_실전_통합_2022.hwp` 1쪽 `문1)`과 첫 수식 사이 간격 불일치 재분석.
- [x] Stage6 비교: PDF/한컴 기준 산출물과 현재 SVG/PNG 비교.
- [x] Stage6 구현: `U+FFFC` 인라인 객체 placeholder가 텍스트 폭에 포함되어 첫 수식이 오른쪽으로 밀리는 문제 보정.
- [x] Stage6 검증: 대상 회귀 테스트와 시각 산출물 확인.
- [x] Stage6 커밋 전 검증: `cargo test --tests`, `wasm-pack build --target web --out-dir pkg` 통과.
- [ ] Stage6 커밋 및 PR 준비.

- [x] Stage7 시작: `3-10월_교육_통합_2022.hwp` 11쪽 미주 간격 이상 재확인.
- [x] Stage7 비교: PDF/한컴 기준 산출물과 현재 SVG/PNG 비교.
- [x] Stage7 구현: 빈 문단 뒤 compact 미주 제목의 추가 40px 완충 제거로 PR #1194 보정.
- [x] Stage7 1차 검증: `cargo test --test issue_1139_inline_picture_duplicate -- --nocapture`, `cargo fmt --all --check` 통과.
- [x] Stage7 커밋 전 검증: `cargo test --tests`, `wasm-pack build --target web --out-dir pkg` 통과.
- [ ] Stage7 커밋 및 PR #1194 브랜치 push.

## Task #1195 — 표 셀 안 TAC 표 앞 빈 줄 미반영 제목·표 겹침 (hcar-001 1쪽) — 완료

- **결과**: 셀[28] "1.동의[필수]" 제목과 4×1 표 겹침 해소. 표 631.8→643.8(제목 639.8 아래). 시각 판정 통과.
- **원인/보정**: `layout_table_cells`(table_layout.rs) 표 분기가 표를 `para_y_before_compose`(첫 줄) 기준 배치 → 표 앞 공백 줄 누락. 이미지 TAC 분기 동형으로 `has_preceding_text && line_segs.len()>1` 시 표 line_seg vpos 상대오프셋 가산(한컴 line-feed 규칙). 가드로 무회귀.
- **검증**: test --tests 1894 passed 0 failed / 6페이지 회귀 p2~p6 무변경 / WASM 빌드 / 좌표 회귀 테스트(보정 OFF 시 FAIL 입증).
- Stage1~4 + 보고서 task_m100_1195_report.md. hcar 샘플 픽스처 커밋(pdf LFS). issue-267 충돌은 무관(devel 정상본 회귀).

(이하 진행 기록)

- 현상: 신청서 12×7 표 셀[28] "1. 개인정보 수집 및 이용 동의[필수]" 제목과 중첩 표가 세로로 겹침(한컴은 빈 문단 간격으로 분리). 작업지시자 한컴 vs rhwp 캡처로 확정.
- 진단(코드 무수정): IR 정상(HWP=HWPX vpos·lh 동일 → 파서 무결). 중첩 표 텍스트는 렌더되나 위치 겹침 → 셀 내부 다문단 `para_y` 누적이 빈 문단(text_len=0, 90% 글자에 따라) 높이를 충분히 전진 못함. `table_cell_content.rs` 654~702 루프 + `layout_composed_paragraph` 빈 문단 height. 트러블슈팅 line_spacing_lineseg_sync(ParaShape/LineSeg 이중구조).
- 이슈 #1195 생성+assign, 브랜치 local/task1195, 수행계획서 task_m100_1195.md. 디버그 SVG: output/poc/issue1195/hcar-001_001.svg.
- HWPX 크로스 체크 완료(동일 결함). 위험 중간(셀 공통 경로) → 구조 가드 우선. 수행계획 승인 → 구현계획서.

## PR #1206 (Task #1134: HWPX serializer 문단 id 충돌, @Mireutale 첫 기여) — CLOSED

- PR #1206 → **CLOSED** (머지 안 함). 이슈 #1134 → OPEN 유지(미해결).
- 내용: (1)문단 id 충돌 수정(context next_para_id 전역 카운터, 본문·셀 공유 — 적절) + (2)header.rs fillBrush 직렬화 완성(winBrush/gradation/imgBrush — PR 본문 미언급, 무관 변경 혼재).
- 닫는 사유: ① 대상이 미완성 모듈(HWPX 쓰기 serialize_hwpx 미완성, 주력은 exportHwp HWPX→HWP #178 어댑터) → 메인테이너 정비 후 진행. ② 무관 변경 혼재. ③ image_fill_mode 토큰(LEFT_BOTTOM/RIGHT_* 등) 자기 HWPX 파서가 못 읽어 라운드트립 비대칭 + 한컴 스펙 미확인(feedback_self_verification_not_hancom).
- brush 역할 확인: control.rs:1854 serialize_shape_fill(IR→HWP)과 같은 IR 공유. brush=도형/셀 배경 채우기, HWP는 숫자코드/HWPX는 문자열토큰.
- 컨트리뷰터에 첫 기여 환영 + id 수정만 분리 재제출 요청. 보고서: pr_1206_report.md.
