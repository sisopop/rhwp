# 2026-06-20 오늘 할일

## 원격 동기화

- [x] `origin` fetch — local/devel·devel `b9466626` 정합 (PR #1442/task_493 등 9커밋 흡수)

## PR #1438 — 폰트 리소스 증명 게이트 glyph 선택 (P27, seo-rii)

- [x] PR 상태 확인 — 이전 CI 실패 후 작성자 수정(0cbfa737), Build & Test pass
- [x] 작성자 응답 확인 — 메인테이너 진단대로 json 테스트 음성/양성 분리
  - 음성: 빈 fontResources → strictVariantAvailable:false + fallbackReason fontFaceMissing
  - 양성(신규): add_portable_font_resources(합성 FontBlob/Face)로 strict 검증 — 라이선스 폰트 없이 CI 안전
- [x] 로컬 재검증 (cherry-pick 4커밋) — paint::json 14/text_v2 15/layer_renderer 33, 전체 lib 1853 passed, fmt/clippy 0
- [x] 리뷰/보고서 `mydocs/pr/archives/pr_1438_{review,report}.md`
- [x] merge 권고 승인 → 작업 커밋 4개 cherry-pick(author seorii 보존) → devel push (1b1cb329)
- [x] PR close + 리뷰 문서 archives (origin/devel 포함 검증)

## #1444 — 확장 viewer CSP 인라인 스크립트 차단 + 다크 아이콘 404 (다크테마 확장 회귀)

- [x] 이슈 등록(#1444) + assignee 지정(edwardkim) + 열린 PR 확인 (겹침 없음)
- [x] `local/task1444` 브랜치 생성 (`local/devel` 기준)
- [x] 트러블슈팅 사전 검색 — 선행 기록 없음
- [x] 근본 원인 진단 (확장 0.2.5 배포 테스트 로그)
  - ① index.html 다크테마 FOUC 방지 인라인 <script>가 확장 CSP(script-src 'self' 'wasm-unsafe-eval') 위반 → 차단
  - ② build.mjs:94가 icon_small_ko.svg(라이트)만 복사, base.css:197 참조 icon_small_ko_dark.svg 누락 → 404
- [x] 수행계획서 `mydocs/plans/task_m100_1444.md` (3단계) — 승인 (`747cb86a`)
- [x] 구현계획서 `mydocs/plans/task_m100_1444_impl.md` (3단계) — 승인 대기
  - 빌드 흐름 확인: vite public/→dist 복사, build.mjs가 dev-tools-inject.js를 src로 주입(CSP 'self' 동작 선례), firefox CSP 동일
- [x] 1단계: public/theme-init.js 분리 + index.html 외부 src + chrome/firefox build.mjs 다크 아이콘 복사 — tsc 0, 인라인 부재 확인 — 승인 대기
- [x] 2단계: build.mjs publicDir:false 정정(theme-init 개별 복사) + dist 검증(theme-init.js·다크아이콘 포함·인라인 부재) + theme-mode e2e 전부 PASS(회귀 0) — 확장 실로드 판정만 잔여 — 승인 대기
- [x] 작업지시자 개발자모드(unpacked) 판정 통과 — CSP/404 실제 미발생, 다크 테마 정상
- [x] 3단계: 트러블슈팅(확장 publicDir:false 함정) + 최종 보고서 — 승인
  - zip 재생성·재제출은 생략(스토어 동일 버전 불가 → 추가 기능과 함께 0.2.6+로 묶음, 작업지시자 결정)
- [x] merge(devel `96a018d5` + main `3b1ae0dd`) + #1444 close (origin/devel 포함 검증)

## #1423 — Cargo.lock git track (재현 가능 빌드, 작업지시자 직접 처리)

- [x] 이슈 확인 + assignee 지정(edwardkim) + 마일스톤 v1.0.0 + 열린 PR 확인 (겹침 없음)
- [x] `local/task1423` 브랜치 생성 (`local/devel` 기준)
- [x] 트러블슈팅 사전 검색 — 선행 기록 없음
- [x] 상태 진단: Cargo.lock은 .gitignore:4(# Rust)에 있어 initial commit부터 미추적. origin/main·devel 둘 다 부재. 로컬 lock은 0.7.16 정합. rhwp는 bin 산출(export-svg/pdf 등) → Cargo 공식 권장상 lock 커밋 대상
- [x] 수행계획서 `mydocs/plans/task_m100_1423.md` (1단계 소형) — 승인 (`4b974117`)
- [x] 1단계: .gitignore 루트 Cargo.lock 제거 + 추적 add + --locked 빌드(lib/bin) + CI 캐시 정상화 + 보고서 (`98e416d7`)
- [x] bindings/Native/Cargo.lock 처리: outdated + 소스 drift(FFI clipboard 3→4 인자, E0061 빌드실패)로 iOS 영역 — 작업지시자 결정 "맥쪽 제외" → .gitignore 명시 제외
- [x] 최종 보고서에 bindings 제외 반영 — 승인 대기
- [ ] 승인 → merge → close
