# 2026-06-18 오늘 할일

## PR #1430 — task 1428: 개체 속성 비율 유지와 누름틀 편집 정합화

- [x] PR 생성 확인 — https://github.com/edwardkim/rhwp/pull/1430
- [x] PR head 확인 — `edwardkim/rhwp:task_m100_1428`, base `edwardkim/rhwp:devel`
- [x] PR 메타 확인
  - 상태: Open, Draft 아님
  - 변경 규모: 30 files, +868 / -130
  - mergeable: `MERGEABLE`
  - merge state: `BLOCKED` (GitHub Actions pending)
- [x] 로컬 필수 검증 확인
  - `git diff --check upstream/devel..HEAD`
  - `cargo fmt --check`
  - `cd rhwp-studio && npx tsc --noEmit`
  - `cd rhwp-studio && npm test`
  - `cargo build --release`
  - `cargo test --release --lib`
  - `cargo test --profile release-test --tests`
  - `cargo clippy --all-targets -- -D warnings`
  - `wasm-pack build --target web --out-dir pkg`
  - `cd rhwp-studio && npm run build`
- [x] Collaborator merge 시뮬레이션
  - `git merge upstream/devel --no-commit --no-ff`
  - 결과: `Already up to date`, 충돌 없음
- [x] 리뷰 문서 archive 생성
  - `mydocs/pr/archives/pr_1430_review.md`
  - `mydocs/pr/archives/pr_1430_review_impl.md`
- [x] 리뷰 문서/오늘할일 커밋 준비
- [ ] 리뷰 문서/오늘할일 push 후 GitHub Actions 재확인
- [ ] merge 여부 판단

## PR #1435 — task 1352: 표 셀 TAC 그림과 텍스트 세로 정렬 보정

- [x] PR 생성 확인 — https://github.com/edwardkim/rhwp/pull/1435
- [x] PR head 확인 — `edwardkim/rhwp:task_m100_1352`, base `edwardkim/rhwp:devel`
- [x] PR 메타 확인
  - 상태: Open, Draft 아님
  - 변경 규모: 19 files, +2282 / -7
  - mergeable: `MERGEABLE`
- [x] 로컬 필수 검증 확인
  - `git diff --check upstream/devel..HEAD`
  - `cargo build --release`
  - `cargo test --release --lib`
  - `cargo test --profile release-test --tests`
  - `cargo fmt --check`
  - `cargo clippy --all-targets -- -D warnings`
  - `wasm-pack build --target web --out-dir pkg`
- [x] 시각 검증 자료 확인
  - `mydocs/report/assets/task_m100_1352_visual_compare/compare/before_after_header_cell_compare.png`
  - `mydocs/report/assets/task_m100_1352_visual_compare/compare/before_after_page1_compare.png`
- [x] 리뷰 문서 archive 생성
  - `mydocs/pr/archives/pr_1435_review.md`
  - `mydocs/pr/archives/pr_1435_review_impl.md`
- [x] 리뷰 문서/오늘할일 커밋 준비
- [ ] 리뷰 문서/오늘할일 push 후 GitHub Actions 재확인
- [ ] merge 여부 판단

## PR #1432 — 차트 샘플 코퍼스 27종 추가 (P-1, refs #1431, johndoekim)

- [x] PR 정보 확인 — base devel, CLEAN, CI 전부 pass, 데이터 전용(+8/-0, 바이너리 81)
- [x] LFS 점검 — pdf/chart 2.5MB(최대 110KB) 50MB 미만, pdf/(일반 git) 배치 정확, LFS 쿼터 무관
- [x] 검증 — 27쌍 완전(hwp 27+hwpx 27), 동적 탐색 테스트 미해당(1 passed), 표본 export-svg 5종 OK, fmt OK
- [x] 리뷰/보고서 `mydocs/pr/archives/pr_1432_{review,report}.md`
- [x] merge 권고 승인 → 작업 커밋 `616aa396` cherry-pick(author johndoekim 보존) → devel push
- [x] PR close + 리뷰 문서 archives

## 메모

- #1428은 PR body에 `Closes #1428`이 있지만 GitHub API의 `closingIssuesReferences`가 비어 있다.
- merge 후 이슈 close 여부를 별도로 확인한다.
- #1431(차트 OLE/OOXML 트래킹)은 #1432 P-1 선행 후에도 유지(닫지 않음).
