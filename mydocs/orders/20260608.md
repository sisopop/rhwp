# 2026-06-08 작업 기록

## PR #1312 / Refs #536 — renderer baseline sweep reports

- [x] PR #1312 메타/패치 검토
- [x] PR branch 로컬 fetch — `local/pr1312-upstream`
- [x] 리뷰 문서 작성 — `mydocs/pr/pr_1312_review.md`
- [x] GitHub checks 확인 — Build & Test, CodeQL, Canvas visual diff, Analyze pass
- [x] Copilot/CodeQL review thread 확인 및 코드 반영 상태 대조
- [x] 반영 완료된 review thread resolve
- [x] 검증 — `cargo fmt --all -- --check`
- [x] 검증 — `cargo check --lib`
- [x] 검증 — `cargo test replay_order --lib`
- [x] 검증 — `node --check rhwp-studio/e2e/helpers.mjs`
- [x] 검증 — `node --check rhwp-studio/e2e/renderer-baseline.mjs`
- [x] 검증 — `node --check rhwp-studio/e2e/renderer-baseline-native-diff.mjs`
- [x] 검증 — `python3 -m json.tool scripts/renderer_baseline_manifest.json`
- [x] 검증 — `python3 scripts/renderer_baseline.py --help`
- [x] 검증 — `python3 scripts/renderer_baseline.py --skip-browser --skip-native --filter paragraph-basic --output /tmp/rhwp-renderer-baseline-smoke-pr1312`
- [x] 검증 — `npm --prefix rhwp-studio run build`
- [x] WASM 보완 검증 — `docker compose --env-file .env.docker run --rm wasm`
- [x] PR #1312 검증 코멘트 작성
- [x] PR #1312 admin merge — `785f5ef7`
- [x] `local/devel` sync
- [x] 리뷰 문서 archive 이동 — `mydocs/pr/archives/pr_1312_review.md`

참고:

- 로컬에는 `wasm-pack` 실행 파일이 없어 PR 본문의 `wasm-pack build --target web --release --no-opt`는 직접 실행하지 못했다.
- 프로젝트 표준 WASM 검증 경로인 Docker WASM 빌드는 성공했다.
- PR #1312는 `Refs #536` 성격이므로 #536은 자동 close 대상이 아니다.

## PR #1327 / Refs #1326 — HWPX inline control serialization

- [x] PR #1327 메타/패치 검토
- [x] PR branch 로컬 fetch — `local/pr1327-upstream`
- [x] 리뷰 문서 작성 — `mydocs/pr/pr_1327_review.md`
- [x] GitHub checks 확인 — Build & Test, CodeQL, Analyze pass
- [x] Copilot unresolved thread 확인 — non-blocking 판정
- [x] 최신 `origin/devel` 병합 시뮬레이션 — conflict 없음
- [x] 검증 — `cargo fmt --all -- --check`
- [x] 검증 — `cargo test --test hwpx_roundtrip_integration`
- [x] 검증 — `cargo clippy --all-targets -- -D warnings`
- [x] maintainer-side 통합 — `d7ff712b`
- [x] `origin/devel` push
- [x] PR #1327 메인테이너 코멘트 작성
- [x] PR #1327 GitHub `MERGED` 상태 확인
- [x] 리뷰 문서 archive 이동 — `mydocs/pr/archives/pr_1327_review.md`

참고:

- PR #1327 head는 `BEHIND` 상태였으므로 GitHub merge 버튼 대신 최신 `devel` 위에 maintainer-side로 통합했다.
- 통합 후 GitHub가 PR #1327을 `MERGED` 상태로 자동 처리했다.
- #1326은 form 관련 후속 범위가 남아 있어 유지한다.
