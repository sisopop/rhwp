# 2026-05-31 — 오늘 할일

## PR #1177 — 표 + picture 한컴 정합 (by @johndoekim, closes #1151)

- [x] PR 정보 확인 — +6244/-323 55파일, CLEAN, CI pass. #1151 3차(#1160→#1173→#1177)
- [x] 재제출 맥락: #1173 컨트리뷰터 자진 close(시각 결함) → 결함 조치 재제출. 작업지시자 외부 위임 결정
- [x] 검토: 4 측면(셀 삽입/토글/sibling 시각/click) + v5~v9 후속 6결함 + helper 정리(v7). 신규 19 테스트
- [x] 검증: auto-merge clean + 빌드 + cargo test --tests 97 스위트 전부 통과 + fmt
- [x] 승인(머지) → devel 머지(84a09e8c) + PR 코멘트 + close #1151 + 보고서 archives
- [x] 시각 판정: 작업지시자 직접 진행 → 통과 (wasm 11:12 빌드, 표+picture 9 시나리오)

## PR #1178 — task 1139 수식/미주 렌더링 + 미주 입력 (by @jangster77) — 보류(규모 과대)

- [x] PR 정보 확인 — +11172/-619 99파일, CONFLICTING/DIRTY, CI 6 pass
- [x] 충돌 진단: PR #1177(표+picture, cc8dee68)과 동일 picture/layout 코드 의미 충돌 (4파일 8 hunk)
- [x] 1-a 직접 결합 시도 → object_ops/dialog 정상 결합, layout 은 두 레이아웃 알고리즘 깊은 통합 + 출력 환경 불안정으로 중단 → 전체 abort (devel 무오염 f8970915)
- [x] 방향 2 전환: @jangster77 에 rebase 요청 코멘트 (충돌 4파일 위치/이유 명시, issue-4585678997)
- [x] 재검토(2026-05-31 18시대): 컨트리뷰터가 rebase 응답 완료 → **MERGEABLE/CLEAN**, merge-base = devel HEAD. head `79846863`(빌드 보정 2커밋), CI 전체 green.
- [x] 실제 규모 재확인 — 99파일 +11234/-653 (소스 ~55파일: renderer/layout·equation·pagination·document_core·paint·wasm_api + rhwp-studio 미주 dialog/도구상자). 레이아웃/조판 코어 심층 변경.
- [x] **작업지시자 "이대로 머지" 결정** → 메인테이너 로컬 `--no-ff` 머지(BEHIND, UI 불가) + 검증 + push.
- [x] 검증: merge CLEAN / fmt / build / test --tests(0 failed) / **WASM Docker 빌드 완료**.
- [x] PR #1178 → **MERGED** (devel `2862206f`), 이슈 #1139 → **CLOSED**(자동). PR 코멘트 + 보고서(pr_1178_report.md).
- [x] 시각·회귀 검증은 메인테이너 수동 진행(작업지시자 지시). 미주 제한 사항은 후속 이슈 대상.
- [x] **작업지시자 시각 판정 + 회귀 테스트 검증 통과** (WASM `pkg/` 19:00 빌드본 기준). PR #1178 처리 종결

## PR #1159 (render: glyph payload resource proof diagnostics, @seo-rii) — 완료

- PR #1159 → **MERGED** (devel `8f3d2e91`). 연결 `Refs #536`(자동 클로즈 아님, z-order 후속은 별도 sweep).
- P19→P20 후속. payload family prefix 로 numeric ref 충돌 방지(`payload_resource_key`), native Skia replay proof contract/construction 분리 보고(glyph-id replay 는 여전히 비활성=proof 단계), schema additive bump(layer 1.15/resource 1.4). studio helper 미러 + 회귀.
- 전부 additive opt-in diagnostics, 기본 renderer 경로 불변, unsupported payload TextRun fallback 유지 → 위험 낮음.
- 검증: 로컬 머지 CLEAN / fmt / clippy(lib) / build / test --tests(**1879 passed, 0 failed**) / native-skia proof 4건 / studio test+build. CI 전부 green.
- BEHIND → 메인테이너 로컬 `--no-ff` 머지 + push(`a2a5593c..8f3d2e91`). PR head 조상 확인 후 PR close(머지 명시). 보고서: pr_1159_report.md.

## PR #1180 (task 1179: Rust 테스트 경고 정리, @jangster77) — 완료

- 연결 이슈 #1179 → CLOSED, PR #1180 → MERGED (devel `f528d40d`)
- 테스트 코드 경고 6건 정리 (중복 #[test], unused_parens, non_snake_case ×2, unused_must_use ×2). 런타임 변경 없음.
- 검증: build / test --tests (lib 1475 + 통합, 0 failed) / clippy(대상 경고 소거) / fmt --check 통과.
- head BEHIND 로 GitHub UI 머지 거부 → 메인테이너 로컬 --no-ff 머지 + push.
- 보고서: mydocs/pr/pr_1180_report.md

## PR #1180 (task 1179: Rust 테스트 경고 정리, @jangster77) — 완료

- 연결 이슈 #1179 → CLOSED, PR #1180 → MERGED (devel `f528d40d`)
- 테스트 코드 경고 6건 정리 (중복 #[test], unused_parens, non_snake_case ×2, unused_must_use ×2). 런타임 변경 없음.
- 검증: build / test --tests (lib 1475 + 통합, 0 failed) / clippy(대상 경고 소거) / fmt --check 통과.
- head BEHIND 로 GitHub UI 머지 거부 → 메인테이너 로컬 --no-ff 머지 + push. 보고서: pr_1180_report.md

## PR #1182 (RawSvg OLE/차트 첫 로드 백지 렌더, @planet6897) — 완료

- 연결 이슈 #1181 → CLOSED, PR #1182 → MERGED (devel `23742708`)
- #1154 v2(PR #1164) flow 디코드 안전망이 PaintOp::Image 만 잡아 RawSvg 누락 → 한셀 OLE/차트/EMF 첫 로드 백지 회귀 보완.
- rendering.rs collect() RawSvg image_count++ / page-renderer.ts data URL 직접 스캔+dedupe.
- 검증: build / test --lib rendering(6) / test --tests(1826, 0 failed) / clippy / fmt / tsc 통과. WASM 빌드 후 시각 판정 통과.
- SVG 내보내기도 OLE 미리보기 PNG 정상 임베드 확인 (output/svg/han-cell-ole/한셀OLE.svg). 보고서: pr_1182_report.md

## PR #1185 (Task #1143: external image bytes injection contract, @postmelee) — 완료

- PR #1185 → MERGED (devel `171bc745`). 관련 이슈 #1143 → CLOSED (작업지시자 승인 후 수동 클로즈).
- #1142(PR #1175) discovery contract 후속. discovery key 로 external image bytes 주입 contract 고정.
- model/document.rs helper 3개 + Vec→BTreeMap, wasm_api injectExternalImageByKey 신규(하위호환), canvaskit_policy payload 기준 direct replay, layout 4경로 external_path 전달, tests/issue_1143.rs 회귀 9건.
- 검증: 클린 머지 / fmt / test --test issue_1143(9) / test --tests(1836, 0 failed). PR 변경 파일 clippy 0건.
- clippy --all-targets -D warnings 실패는 devel baseline 동일(PR 무관) → lint 정리 이슈 #1186 등록(작업지시자 지시). 보고서: pr_1185_report.md

## PR #1190 (Task #1187: BookReview.hwp 글상자 내용 clip 회귀 수정, @postmelee) — 완료

- PR #1190 → **MERGED** (devel `6ca2be7f`). 관련 이슈 #1187 → CLOSED (Closes 미명시 → 수동 클로즈).
- 글상자 내용을 `RenderNodeType::TextBox`(inner_area) clip 노드로 모아 SVG/paint layer/web_canvas 3경로 일관 clip. `ClipKind::TextBox` additive(schema 14→15).
- `layout_composed_paragraph` `suppress_column_top_vpos_fallback` 플래그 — 글상자 호출만 true, 표/footnote 전 호출처 false → **표 셀 기존 동작 보존**. `if !children.is_empty()` 가드로 빈 글상자 clip 회피.
- 검증: 로컬 머지 CLEAN / fmt / clippy(lib) / build / test --tests(**1883 passed, 0 failed**) / svg_snapshot 8 / issue_1187 회귀 2. CI(Canvas visual diff 포함) 전부 green.
- **작업지시자 SVG 시각 판정 통과** (`output/poc/pr1190/BookReview_001.svg`, `textbox-clip-52` bottom 1004.44). web_canvas 변경 포함 → **WASM Docker 빌드 완료**.
- 보고서: pr_1190_report.md / 검토서: pr_1190_review.md

## M100 — v1.0.0 editing foundation

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #1187 | BookReview.hwp 글상자 내용이 영역 밖으로 출력되는 회귀 수정 | 완료 | PR #1190 머지(devel `6ca2be7f`), 시각 판정 통과, 이슈 CLOSED |
