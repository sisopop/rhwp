# 2026-05-30 — 오늘 할일

## Task #1151 — 표 + picture 한컴 정합 (v1+v2+v3+v4+v5+v6+v7 통합 PR)

- 이슈: [#1151](https://github.com/edwardkim/rhwp/issues/1151)
- 마일스톤: M100 / v1.0.0
- 브랜치: `local/task1151`
- 상태: 통합 PR 발행 대기, merge 대기

### 진행 항목

- [x] v4 Stage 7 (셀 안 inline picture click hit-test 완성) 보고서 + commit
- [x] 사용자 시연 (rhwp 신규 표 + 셀 안 이미지 → tac 토글 시 시각 변화 없음) — 결함 보고
- [x] v5 진단: model + composer + paragraph_layout 정상, page tree cache 결함
- [x] v5 Stage 9: `set_picture_properties_native` invalidate_page_tree_cache 누락 fix + regression
- [x] v6 진단 + Stage 10: `Table::update_ctrl_dimensions` self.common 동기화 fix + regression 2 개
- [x] WASM 재빌드 + 사용자 v6 시연 — **정합 통과** ("정확함. rhwp에서 그렇게 만든 문서 한컴에서도 정합")
- [x] v7 audit: Explore agent 2 회 — 9 항목 분석 + 항목 7 anti-pattern 검증
- [x] v7 Stage 12 (항목 1): `CellContext::last_image_indices()` helper
- [x] v7 Stage 13 (항목 4): `parse_cell_path_json` + `resolve_cell_paragraph_mut` helper
- [x] v7 Stage 14 (항목 7): `make_picture_image_node` helper
- [x] v7 Stage 15 (항목 2): 의도된 dual maintenance 주석 보강
- [x] v7 Stage 16 보고서 + v7 통합 최종 보고서 (v1~v7)
- [ ] Stage 17: push origin + upstream devel PR (closes #1151)

### 핵심 변경

- v4: 셀 안 inline picture click hit-test (ImageNode 확장 + 8 caller + by_path API)
- v5: set_picture_properties_native invalidate_page_tree_cache 호출 1 줄 추가
- v6: Table::update_ctrl_dimensions 에서 self.common.width/height 동기화 2 줄 추가
- v7: 4 helper 추출 (CellContext::last_image_indices / parse_cell_path_json /
  resolve_cell_paragraph_mut / make_picture_image_node) + 1 주석 보강.
  순 -48 줄 boilerplate 제거 + 2 의도된 분리 분석 명시.

### 검증

- `cargo test --lib` 1445 passed, 0 failed
- 사용자 시연: v6 fix 적용 후 한컴 정합 + rhwp 산출물 한컴 연 정합 양방향 통과

### 후속 task

- [#1171](https://github.com/edwardkim/rhwp/issues/1171) 사각형 글상자 안 picture click (이중 nested)
