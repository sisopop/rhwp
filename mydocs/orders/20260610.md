# 2026-06-10 작업 기록

| 이슈/PR | 제목 | 상태 | 비고 |
|---|---|---|---|
| #1352 | HWPX 표 셀 TAC 이미지/텍스트 세로 정렬 한컴 정합 | 보류 | 전일 작업 중지, 한컴에디터 조판 확인 대기 |
| PR #1346 | WebCanvas layer adapter 축소 및 option metadata 분리 | 완료 | cherry-pick, 로컬 검증, WASM 빌드, devel push, PR close 완료 |
| PR #1351 | useFontSpace 직렬화 시 항상 false 고정 수정 | 리뷰 승인 | PR 전체 merge 금지, #1350 본질 커밋 선별 수용 승인 |

## Task M100-1352 — 전일 작업 보류

- [x] 전일 작업 중지 확인
- [x] 작업 변경 격리 — `stash@{0}` (`pause-task-m100-1352-before-pr1346`)
- [ ] 한컴에디터 조판 확인 후 재개 여부 결정

## PR #1346 — WebCanvas layer adapter 축소 및 option metadata 분리

- [x] PR #1346 확인 — "render: reduce WebCanvas layer adapter and split option metadata"
- [x] 관련 이슈 확인 — #536 "멀티 렌더러 지원 트래킹 이슈" (open)
- [x] PR 상태 확인:
  - base: `devel`
  - head: `seo-rii:render-p22` (`58168282`)
  - mergeable: `MERGEABLE`
  - changed files: 10, +722 / -631
- [x] GitHub checks 확인:
  - Build & Test — pass
  - Canvas visual diff — pass
  - CodeQL / Analyze rust / Analyze javascript-typescript / Analyze python — pass
  - WASM Build — skipped
- [x] PR head checkout — `local/pr1346-upstream`
- [x] 변경 검토:
  - `src/renderer/web_canvas.rs`
  - `src/paint/json.rs`
  - `src/paint/schema.rs`
  - `src/wasm_api/tests.rs`
  - `tests/render_p22_web_canvas_contract.rs`
  - `rhwp-studio/src/core/types.ts`
  - `rhwp-studio/src/core/wasm-bridge.ts`
  - `rhwp-studio/tests/render-backend.test.ts`
  - `README.md`, `README_EN.md`
- [x] 로컬 검증:
  - `cargo fmt --check` — 통과
  - `CARGO_INCREMENTAL=0 cargo test --lib serializes_layer_option_metadata -- --nocapture` — 통과
  - `CARGO_INCREMENTAL=0 cargo test --lib test_page_layer_tree_export_preserves_output_options -- --nocapture` — 통과
  - `CARGO_INCREMENTAL=0 cargo test --lib layer_tree_schema_constants_match_schema -- --nocapture` — 통과
  - `CARGO_INCREMENTAL=0 cargo test --test render_p22_web_canvas_contract -- --nocapture` — 통과
  - `cd rhwp-studio && node --test tests/render-backend.test.ts` — 23 passed
  - `CARGO_INCREMENTAL=0 cargo check --lib --target wasm32-unknown-unknown -j 2` — 통과
  - `git diff --check local/devel...local/pr1346-upstream` — 통과
- [x] 리뷰 문서 작성 — `mydocs/pr/pr_1346_review.md`
- [x] 기술적 평가 보강 — WebCanvas direct replay / PageLayerTree schema / 호환성 평가
- [x] 작업지시자 리뷰 보고서 승인
- [x] 리뷰 문서 커밋 — `d11253f6`
- [x] PR non-merge commit cherry-pick:
  - `b5edc1db` — refactor(render): replay WebCanvas PaintOps directly
  - `4fb6a74f` — feat(render): split layer option metadata
  - `d5aa4ebc` — fix(render): backfill layer option compatibility mirrors
- [x] cherry-pick 후 검증:
  - `cargo fmt --check` — 통과
  - `git diff --check` — 통과
  - `CARGO_INCREMENTAL=0 cargo test --lib serializes_layer_option_metadata -- --nocapture` — 통과
  - `CARGO_INCREMENTAL=0 cargo test --lib test_page_layer_tree_export_preserves_output_options -- --nocapture` — 통과
  - `CARGO_INCREMENTAL=0 cargo test --lib layer_tree_schema_constants_match_schema -- --nocapture` — 통과
  - `CARGO_INCREMENTAL=0 cargo test --test render_p22_web_canvas_contract -- --nocapture` — 통과
  - `cd rhwp-studio && node --test tests/render-backend.test.ts` — 23 passed
  - `CARGO_INCREMENTAL=0 cargo check --lib --target wasm32-unknown-unknown -j 2` — 통과
- [x] WASM 빌드
  - 1차 실패: Docker daemon 미실행 — `failed to connect to the docker API ... docker.sock ... no such file or directory`
  - Docker Desktop 기동 후 재시도
  - `docker compose --env-file .env.docker run --rm wasm` — 통과 (`Done in 1m 49s`)
- [x] 처리 보고서 작성 — `mydocs/pr/pr_1346_report.md`
- [x] 처리 보고서 승인
- [x] 최종 수용 절차
  - `devel` no-ff merge — `2bc411f4`
  - `origin/devel` push — `2bc411f4`
  - PR #1346 처리 코멘트 작성 — https://github.com/edwardkim/rhwp/pull/1346#issuecomment-4665140905
  - PR #1346 close — `2026-06-09T23:53:48Z`
  - Issue #536 — tracking issue이므로 open 유지

## PR #1351 — useFontSpace 직렬화 시 항상 false 고정 수정

- [x] PR #1351 확인 — "Task #1350 useFontSpace 직렬화 시 항상 false 고정 (IR 값 무시)"
- [x] 관련 이슈 확인 — #1350 "HWPX 시리얼라이저에서 useFontSpace 직렬화 시 항상 false 고정 (IR 값 무시)" (open)
- [x] PR 상태 확인:
  - base: `devel` (`54e6393c`)
  - head: `Martinel2:devel` (`232d6c14`)
  - mergeable: `MERGEABLE`
  - changed files: 18, +1176 / -2
- [x] GitHub checks 확인:
  - Build & Test — pass
  - CodeQL / Analyze rust / Analyze javascript-typescript / Analyze python — pass
  - WASM Build — skipped
- [x] PR head checkout — `local/pr1351-upstream`
- [x] 변경 범위 검토:
  - PR 전체 diff에는 #1298/#1321 과거 작업 문서/코드가 섞여 있음
  - 현재 `local/devel` 대비 PR head 직접 병합은 대량 삭제/되돌림 위험
  - #1350 본질 커밋은 `d9107849` 단일 커밋으로 식별
- [x] 단일 커밋 적용 가능성 확인:
  - `git show --format= d9107849 -- ... | git apply --check` — 통과
- [x] 리뷰 문서 작성 — `mydocs/pr/pr_1351_review.md`
- [x] 작업지시자 리뷰 보고서 승인
- [ ] `d9107849` 단일 cherry-pick
- [ ] HWP5 serializer bit 25 보강 검토
- [ ] 검증 및 처리 보고서 작성
