# 2026-06-04 작업 기록

| 이슈/PR | 제목 | 상태 | 비고 |
|---|---|---|---|
| #1285 | TAC 표 2개가 한 셀에 배치된 오른쪽 정렬 문단에서 inline 폭 계산 오류 | 완료 | SVG/웹 시각 판정 통과 |
| #823 | PNG 렌더가 headless macOS에서 미설치 폰트 fallback 시 hang | 완료 승인 | native-skia 폰트 lookup 차단, release lib 테스트 및 macOS 직접 확인 통과 |

## Task M100-823 — headless macOS PNG 렌더 CoreText downloadable font lookup hang 방지

- [x] GitHub Issue #823 확인 — "[bug, CI 한정] PNG 렌더가 headless macOS 에서 hang"
- [x] 열린 PR 검색 — #823 / CoreText / fontd / match_family_style 관련 열린 PR 없음
- [x] GitHub assignee 지정 — `gh issue edit 823 --add-assignee edwardkim`
- [x] 작업 브랜치 생성 — `local/task_m100_823`
- [x] 현행 호출부 조사:
  - `src/renderer/skia/renderer.rs::SkiaLayerRenderer`
  - `src/renderer/skia/text_replay.rs` 본문 fallback chain + 문단/제어문자 mark font
  - `src/renderer/skia/renderer.rs::make_form_font`
  - `src/renderer/skia/equation_conv.rs::draw_text`
- [x] 수행 계획서 작성 — `mydocs/plans/task_m100_823.md`
- [x] 구현 계획서 작성 — `mydocs/plans/task_m100_823_impl.md`
- [x] 작업지시자 승인 — 구현 계획서 승인
- [x] 구현 완료:
  - `src/renderer/skia/font_lookup.rs` 공통 helper 추가
  - `SkiaLayerRenderer` 생성 시 `FontMgr::family_names()` 시스템 family 캐시 수집
  - 본문 text replay fallback chain의 시스템 lookup 사전 필터링
  - mark font `DejaVu Sans`, form control CJK fallback, equation fallback 경로 필터링
- [x] 검증 완료:
  - `cargo fmt --check`
  - `cargo check --lib`
  - `cargo check --lib --features native-skia`
  - `cargo test --lib --features native-skia font_lookup`
  - `cargo test --release --lib --features native-skia font_lookup`
  - `cargo test --release --lib` — 1562 passed, 6 ignored
- [x] 직접 `match_family_style` 호출 확인 — `src/renderer/skia/font_lookup.rs` helper 한 곳만 남김
- [x] Stage 보고서 작성 — `mydocs/working/task_m100_823_stage1.md`
- [x] 최종 보고서 작성 — `mydocs/report/task_m100_823_report.md`
- [x] 작업지시자 macOS 직접 테스트 확인 — PNG export hang 없이 완료
- [x] 다음 절차 진행 승인
